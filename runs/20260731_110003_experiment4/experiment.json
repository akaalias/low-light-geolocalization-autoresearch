{
  "title": "Local belief pooling decode: confidence = softmax mass on the 3x3 cell neighborhood, fix = its mass-weighted centroid",
  "category": "architecture",
  "hypothesis": "Experiment 3's champion is accurate but needlessly silent: of the 0.1827 mission score, 0.1475 is abstention and only 0.0175 is false fixes, while its confident frames are 98% accurate (usable 0.835 vs false 0.0175). Eval views stand 11-17 m off-lattice, so a view near a cell border legitimately splits softmax mass between adjacent 128 m cells; with confidence defined as mass on the single argmax cell, a 2-way border split can and a 4-way corner split must fall below the scorer's 0.3 threshold even though every competing hypothesis names the same neighborhood. That is label ambiguity, not place ambiguity, and the current decode cannot tell them apart. Pooling belief over the 3x3 block centred on the argmax cell makes confidence measure 'does the model know WHERE it is' rather than 'does it know which arbitrary tile label applies', converting adjacent-split abstentions into fixes while leaving genuine far-field ambiguity (mass scattered across distant cells) below threshold. Placing the fix at the mass-weighted centroid of that local block should also cut error near borders, since the centroid interpolates between competing cell centres instead of snapping to one.",
  "method": "Change ONLY the deployed decode in model/model.py's CellLocNet.forward(); training_outputs(), loss_fn, train.py, and the full-lattice training schedule stay byte-identical to the exp-3 champion. New forward(): p = softmax(logits); idx = argmax; build a [B, n_cells] mask selecting cells whose centre lies within 1.6 cell-pitches (Chebyshev, per axis) of the winning cell's centre — computed on the fly from the existing cell_u/cell_v buffers with Sub/Abs/Less/Mul ops so it exports cleanly to ONNX and stays edge-safe with no duplicate counting; q = p * mask; conf = sum(q); fix = sum(q/conf * cell centres) + the existing tanh offset head contribution, clamped to [0,1] as before. conf is a sub-sum of a softmax so it remains in [0,1] and is >= the old single-cell conf by construction: abstentions can only convert, never appear.",
  "expected_outcome": "mission_score drops from 0.1827 to <= 0.135, target ~0.10. Mechanically: abstain_rate 0.1475 -> <= 0.06 (adjacent splits rescued; genuinely ambiguous frames stay abstained), usable_fix_rate 0.835 -> >= 0.90, false_fix_rate 0.0175 -> <= 0.035 (some rescued frames will be wrong; the 98% accuracy of currently-confident frames bounds the risk). Standing metric check: the gain must appear in usable_fix_rate itself. Secondary: median error should fall from 48 m toward ~35 m via centroid interpolation (logged only). Informative null: if abstain_rate barely moves, the abstentions are far-field aliasing rather than adjacent splits, redirecting the next experiment at trunk/descriptor capacity. Gates unaffected: adds only [1, 2970]-sized elementwise ops (model 3.05 MB < 4 MiB, latency 1.6 ms << 250 ms).",
  "init_strategy": "from-scratch",
  "eli5": "The model localizes by picking which 128-metre map tile it is over, like answering a multiple-choice question, and it only speaks up when one answer gets enough of its vote. But when the drone flies near the border between two tiles, the model honestly splits its vote between them — and then stays silent, even though both answers point at the same street corner. That silence was most of our remaining score. Now neighbouring tiles get to pool their votes: if the belief lands in one tight cluster on the map, we place the fix at the cluster's balance point and speak up; if it is scattered across the city, we stay silent exactly as before.",
  "architecture": {
    "stages": [
      {"name": "Camera frame", "detail": "128×128 px daytime crop, Berlin only, no synthetic lighting variants", "changed": false},
      {"name": "Feature extractor", "detail": "small strided conv stack that turns the frame into a compact texture fingerprint (last layer 160 channels)", "changed": false},
      {"name": "Map-cell classifier", "detail": "scores all ~3,000 128 m map tiles of Berlin and picks the tile this view belongs to — localization as multiple choice, not coordinate guessing", "changed": false},
      {"name": "Local belief pooling", "detail": "NEW: gathers the belief spread over the winning tile AND its 8 neighbours, and places the fix at the balance point of that local cluster instead of snapping to one tile's centre", "changed": true},
      {"name": "Within-cell offset", "detail": "small head that nudges the fix from the pooled balance point to the exact spot", "changed": false},
      {"name": "Confidence gate", "detail": "confidence = the belief mass on the winning tile plus its 8 neighbours — a vote split across adjacent tiles still counts as knowing where you are; belief scattered across distant tiles still reads as unsure and the drone abstains", "changed": true},
      {"name": "Training signal", "detail": "cross-entropy on the true tile + offset loss; every epoch now walks the ENTIRE 72,712-position Berlin lattice with fresh rotations (581k total views, ~196 per tile — 12× exp 2's coverage), cosine LR decay for the longer run", "changed": false, "train_only": true}
    ]
  },
  "implementation_brief": "Edit model/model.py ONLY; model/train.py is untouched. Within model.py, change ONLY CellLocNet.forward(). Everything else — __init__ (trunk, heads, cell_u/cell_v buffers, scale_u/scale_v), training_outputs(), loss_fn(), build_model(), export_onnx(), estimate_position(), CELL_PX, grid_dims — stays byte-identical, so training and the loss are exactly the exp-3 champion's. New forward(x): (1) logits, offset = self.training_outputs(x); p = F.softmax(logits, dim=1)  # [B, C]. (2) idx = p.argmax(dim=1); win_u = self.cell_u.index_select(0, idx); win_v = self.cell_v.index_select(0, idx)  # [B]. (3) Neighborhood mask over cell centres, edge-safe and duplicate-free, ONNX-friendly (no python loops, no gather of index tables): du = (self.cell_u.unsqueeze(0) - win_u.unsqueeze(1)).abs(); dv = (self.cell_v.unsqueeze(0) - win_v.unsqueeze(1)).abs(); mask = ((du <= 1.6 * self.scale_u) & (dv <= 1.6 * self.scale_v)).float()  # [B, C]; 1.6 cell-pitches per axis selects exactly the 3×3 block with float-safety margin (next ring is at 2.0 pitches). (4) q = p * mask; conf = q.sum(dim=1)  # [B], guaranteed in [0,1] and >= single-cell max. (5) w = q / conf.unsqueeze(1).clamp_min(1e-9); u = (w * self.cell_u.unsqueeze(0)).sum(dim=1) + offset[:, 0] * self.scale_u; v = (w * self.cell_v.unsqueeze(0)).sum(dim=1) + offset[:, 1] * self.scale_v. (6) return torch.stack([u.clamp(0.0, 1.0), v.clamp(0.0, 1.0), conf], dim=1)  # [B, 3]. Use only ops that torch.onnx.export handles at opset 17 with dynamo=False (sub/abs/le/logical_and/cast-via-.float()/mul/sum/argmax/index_select/clamp — all fine; if logical_and export complains, multiply the two .float() masks instead of &). CONTRACTS TO PRESERVE: train.py's CLI is unchanged (do not touch it); the ONNX export contract in model.py's docstring holds exactly — one ONNX per area at <out>/models/<area>.onnx, input 1×3×128×128 float32 in [0,1], output [[u, v, conf]] with u,v normalized map coords and conf in [0,1]; the whole decode (softmax → argmax → neighborhood mask → pooled centroid → offset → conf) must live INSIDE the exported graph because the frozen scorer runs the ONNX directly at pipeline/score.py's CONF_THRESHOLD=0.3. Update model.py's module docstring 'Current:' paragraph and forward()'s comment to describe the pooled decode (confidence = 3×3-neighborhood mass, fix = local mass-weighted centroid + offset). Keep the offset application exactly as today (added in normalized coords, scaled by scale_u/scale_v) — it is trained as a within-true-cell offset and continues to apply unchanged on top of the pooled centroid."
}
